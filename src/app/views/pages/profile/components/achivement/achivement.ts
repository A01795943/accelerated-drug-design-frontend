import { Component, CUSTOM_ELEMENTS_SCHEMA } from '@angular/core';

@Component({
  selector: 'profile-achievement',
  standalone: true,
  imports: [],
  templateUrl: './achivement.html',
  schemas: [CUSTOM_ELEMENTS_SCHEMA],
})
export class Achievement {}
